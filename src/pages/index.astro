---
// src/pages/index.astro
import { getCollection } from 'astro:content';
import { siteDataEn, siteDataZh } from '../data/siteData';
import '../styles/global.css';

// 导入本地图片
// 1. 研发基地
import workshop1 from '../assets/images/workshop-1.jpg';
import workshop101 from '../assets/images/workshop-101.jpg';

// 2. 无尘车间
import workshop2 from '../assets/images/workshop-2.jpg';
import workshop201 from '../assets/images/workshop-201.jpg';
import workshop202 from '../assets/images/workshop-202.jpg';
import workshop203 from '../assets/images/workshop-203.jpg';

// 3. 精密装备
import workshop3 from '../assets/images/workshop-3.jpg';
import workshop301 from '../assets/images/workshop-301.jpg';
// 4. 智能库房
import workshop4 from '../assets/images/workshop-4.jpg';
import workshop401 from '../assets/images/workshop-401.jpg';


import logoImg from '../assets/images/logo.png';

// 导入产品图片 (你可以引入你新准备的 90kWh 和 60kWh 本地图片)
import hero1 from '../assets/images/cp_1.jpg';
import hero2 from '../assets/images/cp_2.jpg';
import hero3 from '../assets/images/cp_3.jpg';
import hero4 from '../assets/images/cp_4.jpg';
import hero5 from '../assets/images/cp_5.jpg';
import hero6 from '../assets/images/cp_6.jpg';

// 👈 1. 为 90kWh 旗舰版配置专属图片组
const gallery90kWh = [hero1, hero2, hero3];

// 👈 2. 为 60kWh 轻量版配置专属图片组 (如果有新照片，直接 import 进来填在这里)
const gallery60kWh = [hero4, hero5, hero6]; 

// 组合为多产品图片组
const productGalleries = [gallery90kWh, gallery60kWh];

// 导入 Banner 背景大图与缩略图
import heroBg1 from '../assets/images/hero-bg.png';
import heroBg2 from '../assets/images/hero-bg2.png';
import heroBg3 from '../assets/images/hero-bg3.png';

import cpImg1 from '../assets/images/cp_img1.jpg';
import cpImg2 from '../assets/images/cp_img2.jpg';
import cpImg3 from '../assets/images/cp_img3.jpg';
import cpImg4 from '../assets/images/cp_img4.jpg';
import cpImg5 from '../assets/images/cp_img5.jpg';
import cpImg6 from '../assets/images/cp_img6.jpg';
import cpImg7 from '../assets/images/cp_img7.jpg';
import cpImg8 from '../assets/images/cp_img8.jpg';
import cpImg9 from '../assets/images/cp_img9.jpg';

// 绑定 3 页 Banner 的图片与右下角缩略图资源
const heroSlidesAssets = [
  { bg: heroBg1, gallery: [cpImg1, cpImg2, cpImg3] },
  { bg: heroBg2, gallery: [cpImg4, cpImg5, cpImg6] },
  { bg: heroBg3, gallery: [cpImg7, cpImg8, cpImg9] },
];

// 👈 为 4 个分类各配置 3 张照片 (你可以将后面的图片换成你真实准备好的其他照片文件名)
const workshopGalleries = [
  [workshop1, workshop101], // 1. 研发基地：3张照片
  [workshop2, workshop201, workshop202, workshop203], // 2. 无尘车间：3张照片
  [workshop3, workshop301], // 3. 精密装备：3张照片
  [workshop4, workshop401]  // 4. 智能库房：3张照片
];

// 读取 Markdown 轮播集合，分别提取英文和中文 Slide
const rawSlides = await getCollection('hero');
const slidesEn = rawSlides
  .filter(item => item.data.lang === 'en')
  .sort((a, b) => a.data.order - b.data.order);

const slidesZh = rawSlides
  .filter(item => item.data.lang === 'zh')
  .sort((a, b) => a.data.order - b.data.order);

const pageTitle = `${siteDataEn.siteName} | Professional Industrial & ESS Lithium Solution Specialist`;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <title>{pageTitle}</title>
  </head>
  <body class="lang-en">
    <!-- 顶部导航栏 -->
    <header class="top-nav">
      <div class="nav-container">
        <div class="logo">
          <img src={logoImg.src} alt={`${siteDataEn.siteName} ${siteDataEn.siteSubTitle}`} class="logo-img" />
          <div class="logo-text">
            <span class="logo-title">
              <span class="lang-en">{siteDataEn.siteName}</span>
              <span class="lang-zh">{siteDataZh.siteName}</span>
            </span>
            <span class="logo-sub">{siteDataEn.siteSubTitle}</span>
          </div>
        </div>

        <nav class="nav-links">
          {siteDataEn.nav.links.map((link, idx) => (
            <a href={link.href} class="nav-link">
              <span class="lang-en">{link.name}</span>
              <span class="lang-zh">{siteDataZh.nav.links[idx].name}</span>
            </a>
          ))}
        </nav>

        <div class="nav-actions">
          <button class="lang-btn" id="lang-toggle-btn">
            <span class="lang-icon">🌐</span>
            <span class="lang-text">中文</span>
          </button>
          <button class="btn-quote trigger-quote-modal">
            <span class="lang-en">{siteDataEn.nav.quoteBtnText}</span>
            <span class="lang-zh">{siteDataZh.nav.quoteBtnText}</span>
          </button>
        </div>
      </div>
    </header>

    <!-- 右侧悬浮小点导航 -->
    <aside class="dot-nav">
      {siteDataEn.nav.links.map((link, idx) => (
        <a href={link.href} class={`dot-item ${idx === 0 ? 'active' : ''}`} data-section={link.href.replace('#', '')}>
          <span class="tooltip">
            <span class="lang-en">{link.name}</span>
            <span class="lang-zh">{siteDataZh.nav.links[idx].name}</span>
          </span>
          <span class="dot"></span>
        </a>
      ))}
    </aside>

    <main class="content">
      <!-- Section 1: Hero 全屏轮播区域 (直接读取 siteData.hero 变量) -->
      <section id="hero" class="section hero-section">
        <div class="slider-container">
          {siteDataEn.hero.map((slideEn, index) => {
            const slideZh = siteDataZh.hero[index];
            const asset = heroSlidesAssets[index] || heroSlidesAssets[0];
            return (
              <div class={`slide ${index === 0 ? 'active' : ''}`} data-index={index}>
                <!-- 背景图与蒙版 -->
                <div class="slide-bg" style={`background-image: url('${asset.bg.src}');`}></div>
                <div class="hero-mask"></div>

                <!-- 英文文案 -->
                <div class="hero-content lang-en">
                  <span class={`pill-badge badge-${slideEn.badgeColor || 'emerald'}`}>{slideEn.badge}</span>
                  <h1>{slideEn.title}<br/><span>{slideEn.subtitle}</span></h1>
                  <p class="subtitle">{slideEn.description}</p>
                  <div class="hero-btns">
                    <a href={slideEn.ctaLink1} class="btn btn-green">{slideEn.ctaText1}</a>
                    <button class="btn btn-translucent trigger-quote-modal">{slideEn.ctaText2}</button>
                  </div>
                </div>

                <!-- 中文文案 -->
                <div class="hero-content lang-zh">
                  <span class={`pill-badge badge-${slideZh.badgeColor || 'emerald'}`}>{slideZh.badge}</span>
                  <h1>{slideZh.title}<br/><span>{slideZh.subtitle}</span></h1>
                  <p class="subtitle">{slideZh.description}</p>
                  <div class="hero-btns">
                    <a href={slideZh.ctaLink1} class="btn btn-green">{slideZh.ctaText1}</a>
                    <button class="btn btn-translucent trigger-quote-modal">{slideZh.ctaText2}</button>
                  </div>
                </div>

                <!-- 右下角缩略图 -->
                <div class="hero-thumbnails">
                  {asset.gallery.map((img) => (
                    <div class="thumb-card" data-full={img.src}>
                      <img src={img.src} alt="Thumb" />
                    </div>
                  ))}
                </div>
              </div>
            );
          })}
        </div>

        <button class="slider-arrow arrow-left" id="prev-slide">‹</button>
        <button class="slider-arrow arrow-right" id="next-slide">›</button>

        <div class="slider-dots">
          {siteDataEn.hero.map((_, index) => (
            <span class={`slider-dot ${index === 0 ? 'active' : ''}`} data-index={index}></span>
          ))}
        </div>
      </section>

      <!-- Section 2: 公司简介 -->
      <section id="about" class="section about-section">
        <div class="section-inner">
          <span class="pill-badge-center">
            <span class="lang-en">{siteDataEn.about.badge}</span>
            <span class="lang-zh">{siteDataZh.about.badge}</span>
          </span>
          <h2>
            <span class="lang-en">{siteDataEn.about.title}</span>
            <span class="lang-zh">{siteDataZh.about.title}</span>
          </h2>
          <p class="section-desc">
            <span class="lang-en">{siteDataEn.about.subTitle}</span>
            <span class="lang-zh">{siteDataZh.about.subTitle}</span>
          </p>

          <div class="grid-2 about-main-grid">
            <div class="text-block">
              <h3>
                <span class="lang-en">{siteDataEn.about.mainTitle}</span>
                <span class="lang-zh">{siteDataZh.about.mainTitle}</span>
              </h3>
              <div class="lang-en">
                {siteDataEn.about.paragraphs.map(p => <p>{p}</p>)}
              </div>
              <div class="lang-zh">
                {siteDataZh.about.paragraphs.map(p => <p>{p}</p>)}
              </div>
            </div>
            <!-- 工厂实景宣传视频展示区 (带高质感封面与 ▶ 播放按钮) -->
            <!-- 工厂实景宣传视频展示区 (带放大全屏播放按钮) -->
            <!-- 工厂实景宣传视频展示区 (文案提取至 siteData.ts 并支持双语) -->
          <div class="factory-video-section">
            <div class="video-player-box" id="video-wrapper">
              <video 
                id="factory-promo-video" 
                controls 
                preload="metadata" 
                poster="/video-poster.jpg"
              >
                <source src="/factory-video.mp4" type="video/mp4" />
              </video>

              <!-- 封面居中 ▶ 播放按钮 -->
              <div class="video-play-trigger" id="custom-video-play-btn">
                <div class="play-btn-circle">
                  <span class="play-icon">▶</span>
                </div>
                <span class="play-btn-text">
                  <span class="lang-en">{siteDataEn.about.factoryVideo.playBtnText}</span>
                  <span class="lang-zh">{siteDataZh.about.factoryVideo.playBtnText}</span>
                </span>
              </div>

              <!-- 左上角标签 -->
              <div class="video-overlay-tag">
                <span>🎬 
                  <span class="lang-en">{siteDataEn.about.factoryVideo.tag}</span>
                  <span class="lang-zh">{siteDataZh.about.factoryVideo.tag}</span>
                </span>
              </div>

              <!-- 右上角：放大全屏播放按钮 -->
              <button class="video-expand-btn trigger-video-modal">
                <span>⛶ 
                  <span class="lang-en">{siteDataEn.about.factoryVideo.expandBtnText}</span>
                  <span class="lang-zh">{siteDataZh.about.factoryVideo.expandBtnText}</span>
                </span>
              </button>
            </div>
          </div>

            <!-- ... (其他内容) ... -->

            <!-- 10. 全屏高清视频弹窗 (Video Modal) -->
            <div id="video-modal" class="video-modal">
              <div class="video-modal-card">
                <button id="close-video-modal" class="video-close-btn">✕</button>
                <div class="modal-video-wrapper">
                  <video id="modal-factory-video" controls preload="metadata" poster="/video-poster.jpg">
                    <source src="/factory-video.mp4" type="video/mp4" />
                  </video>
                </div>
              </div>
            </div>
          </div>
          <!-- 硬件实力展示 (升级为 4 个独立小轮播卡片) -->
          <div class="factory-showcase">
            <h3 class="showcase-title">
              <span class="lang-en">{siteDataEn.about.showcaseTitle}</span>
              <span class="lang-zh">{siteDataZh.about.showcaseTitle}</span>
            </h3>
            <div class="factory-grid">
              {siteDataEn.about.showcaseList.map((itemEn, idx) => {
                const itemZh = siteDataZh.about.showcaseList[idx];
                const images = workshopGalleries[idx] || [workshop1];
                return (
                  <div class="factory-card factory-slider" data-card-idx={idx}>
                    <!-- 多图轮播层 -->
                    <div class="factory-slides">
                      {images.map((img, imgIdx) => (
                        <div 
                          class={`factory-slide ${imgIdx === 0 ? 'active' : ''}`}
                          data-full={img.src}
                        >
                          <img src={img.src} alt={itemEn.title} />
                        </div>
                      ))}
                    </div>

                    <!-- 迷你切换箭头 (只有多于1张图时才显示) -->
                    {images.length > 1 && (
                      <>
                        <button class="f-arrow f-arrow-left">‹</button>
                        <button class="f-arrow f-arrow-right">›</button>
                        <div class="f-dots">
                          {images.map((_, dotIdx) => (
                            <span class={`f-dot ${dotIdx === 0 ? 'active' : ''}`} data-fdot={dotIdx}></span>
                          ))}
                        </div>
                      </>
                    )}

                    <!-- 底部文字面板 -->
                    <div class="factory-info">
                      <span class="factory-tag">
                        <span class="lang-en">{itemEn.tag}</span>
                        <span class="lang-zh">{itemZh.tag}</span>
                      </span>
                      <h4>
                        <span class="lang-en">{itemEn.title}</span>
                        <span class="lang-zh">{itemZh.title}</span>
                      </h4>
                    </div>
                  </div>
                );
              })}
            </div>
          </div>
        </div>
      </section>

      <!-- Section 3: 船舶锂电 (多产品按型号独立显示图片) -->
      <section id="products" class="section bg-light">
        <div class="section-inner">
          <span class="pill-badge-center">
            <span class="lang-en">{siteDataEn.products.badge}</span>
            <span class="lang-zh">{siteDataZh.products.badge}</span>
          </span>
          <h2>
            <span class="lang-en">{siteDataEn.products.title}</span>
            <span class="lang-zh">{siteDataZh.products.title}</span>
          </h2>
          <p class="section-desc">
            <span class="lang-en">{siteDataEn.products.subTitle}</span>
            <span class="lang-zh">{siteDataZh.products.subTitle}</span>
          </p>

          <!-- 外层多产品轮播容器 -->
          <div class="product-card-carousel-wrapper">
            <button class="product-nav-arrow product-nav-prev" id="product-prev-btn">‹</button>
            <button class="product-nav-arrow product-nav-next" id="product-next-btn">›</button>

            <div class="product-card-slides">
              {siteDataEn.products.list.map((prodEn, prodIdx) => {
                const prodZh = siteDataZh.products.list[prodIdx];
                // 关键改动：根据当前产品索引 prodIdx 获取该产品专属的图片组！
                const currentGallery = productGalleries[prodIdx] || productGalleries[0];

                return (
                  <div class={`product-card-slide ${prodIdx === 0 ? 'active' : ''}`} data-product-index={prodIdx}>
                    <div class="product-showcase-card">
                      <!-- 1. 全宽绿色顶部标题栏 -->
                      <div class="product-card-top-header">
                        <h3 class="product-top-title">
                          <span class="lang-en">{prodEn.mainCardTitle}</span>
                          <span class="lang-zh">{prodZh.mainCardTitle}</span>
                        </h3>
                        <span class="product-top-model">
                          <span class="lang-en">{prodEn.model}</span>
                          <span class="lang-zh">{prodZh.model}</span>
                        </span>
                      </div>

                      <!-- 2. 下方主体 -->
                      <div class="product-card-body">
                        <!-- 左侧：当前产品专属的图片轮播 -->
                        <div class="product-image-box product-slider-box">
                          <div class="product-slides">
                            {currentGallery.map((imgItem, imgIdx) => (
                              <div class={`product-slide ${imgIdx === 0 ? 'active' : ''}`} data-full={imgItem.src}>
                                <img src={imgItem.src} alt={prodEn.mainCardTitle} />
                              </div>
                            ))}
                          </div>

                          <div class="image-overlay-badge">
                            <span class="lang-en">{prodEn.overlayBadge}</span>
                            <span class="lang-zh">{prodZh.overlayBadge}</span>
                          </div>

                          {currentGallery.length > 1 && (
                            <>
                              <button class="p-arrow p-arrow-left">‹</button>
                              <button class="p-arrow p-arrow-right">›</button>

                              <div class="p-slider-dots">
                                {currentGallery.map((_, dotIdx) => (
                                  <span class={`p-dot ${dotIdx === 0 ? 'active' : ''}`} data-pindex={dotIdx}></span>
                                ))}
                              </div>
                            </>
                          )}
                        </div>

                        <!-- 右侧：详情参数与卖点 -->
                        <div class="product-info-box">
                          <div class="product-specs-grid">
                            {prodEn.specs.map((spec, sIdx) => (
                              <div class="spec-item">
                                <span class="spec-val">{spec.val}</span>
                                <span class="spec-label">
                                  <span class="lang-en">{spec.label}</span>
                                  <span class="lang-zh">{prodZh.specs[sIdx].label}</span>
                                </span>
                              </div>
                            ))}
                          </div>

                          <ul class="product-features">
                            {prodEn.features.map((fEn, fIdx) => {
                              const fZh = prodZh.features[fIdx];
                              return (
                                <li>
                                  <span class="check-badge">✓</span>
                                  <div>
                                    <strong>
                                      <span class="lang-en">{fEn.bold}</span>
                                      <span class="lang-zh">{fZh.bold}</span>
                                    </strong>
                                    <span>
                                      <span class="lang-en">{fEn.text}</span>
                                      <span class="lang-zh">{fZh.text}</span>
                                    </span>
                                  </div>
                                </li>
                              );
                            })}
                          </ul>

                          <div class="product-actions">
                            <button class="btn btn-green trigger-specs-modal">
                              <span class="lang-en">{prodEn.ctaBtnText}</span>
                              <span class="lang-zh">{prodZh.ctaBtnText}</span>
                            </button>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                );
              })}
            </div>

            <!-- 底部产品切换 Tabs -->
            <div class="product-carousel-tabs">
              {siteDataEn.products.list.map((prodEn, prodIdx) => {
                const prodZh = siteDataZh.products.list[prodIdx];
                return (
                  <button class={`product-tab-btn ${prodIdx === 0 ? 'active' : ''}`} data-tab-index={prodIdx}>
                    <span class="lang-en">{prodEn.model}</span>
                    <span class="lang-zh">{prodZh.model}</span>
                  </button>
                );
              })}
            </div>
          </div>
        </div>
      </section>

      <!-- Section 4: 核心优势 -->
      <section id="advantages" class="section advantages-section">
        <div class="section-inner">
          <span class="pill-badge-center">
            <span class="lang-en">{siteDataEn.advantages.badge}</span>
            <span class="lang-zh">{siteDataZh.advantages.badge}</span>
          </span>
          <h2>
            <span class="lang-en">{siteDataEn.advantages.title}</span>
            <span class="lang-zh">{siteDataZh.advantages.title}</span>
          </h2>
          <p class="section-desc">
            <span class="lang-en">{siteDataEn.advantages.subTitle}</span>
            <span class="lang-zh">{siteDataZh.advantages.subTitle}</span>
          </p>

          <div class="advantages-grid">
            {siteDataEn.advantages.items.map((item, idx) => (
              <div class="adv-card">
                <div class="adv-header">
                  <div class="adv-icon-box">{item.icon}</div>
                  <span class="adv-num">{item.num}</span>
                </div>
                <h3>
                  <span class="lang-en">{item.title}</span>
                  <span class="lang-zh">{siteDataZh.advantages.items[idx].title}</span>
                </h3>
                <p>
                  <span class="lang-en">{item.desc}</span>
                  <span class="lang-zh">{siteDataZh.advantages.items[idx].desc}</span>
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- Section 5: 联系我们 -->
      <section id="contact" class="section contact-section">
        <div class="section-inner">
          <span class="pill-badge-center">
            <span class="lang-en">{siteDataEn.contact.badge}</span>
            <span class="lang-zh">{siteDataZh.contact.badge}</span>
          </span>
          <h2>
            <span class="lang-en">{siteDataEn.contact.title}</span>
            <span class="lang-zh">{siteDataZh.contact.title}</span>
          </h2>
          <p class="section-desc">
            <span class="lang-en">{siteDataEn.contact.subTitle}</span>
            <span class="lang-zh">{siteDataZh.contact.subTitle}</span>
          </p>

          <div class="contact-grid">
            {siteDataEn.contact.items.map((item, idx) => (
              <div class={`contact-card ${item.btnText ? 'contact-card-highlight' : ''}`}>
                <div class="contact-icon">{item.icon}</div>
                <h3>
                  <span class="lang-en">{item.title}</span>
                  <span class="lang-zh">{siteDataZh.contact.items[idx].title}</span>
                </h3>
                {item.desc && (
                  <p>
                    <span class="lang-en">{item.desc}</span>
                    <span class="lang-zh">{siteDataZh.contact.items[idx].desc}</span>
                  </p>
                )}
                {item.btnText && (
                  <button class="btn btn-green trigger-quote-modal">
                    <span class="lang-en">{item.btnText}</span>
                    <span class="lang-zh">{siteDataZh.contact.items[idx].btnText}</span>
                  </button>
                )}
              </div>
            ))}
          </div>
        </div>
      </section>
    </main>

    <!-- 6. 底部 Footer -->
    <footer class="site-footer">
      <div class="footer-container">
        <div class="footer-col footer-brand">
          <div class="footer-logo">
            <span class="logo-icon">🔋</span>
            <span class="logo-text">
              <span class="lang-en">{siteDataEn.siteName}</span>
              <span class="lang-zh">{siteDataZh.siteName}</span>
            </span>
          </div>
          <p class="brand-desc">
            <span class="lang-en">{siteDataEn.footer.brandDesc}</span>
            <span class="lang-zh">{siteDataZh.footer.brandDesc}</span>
          </p>
        </div>

        <div class="footer-col">
          <h4>
            <span class="lang-en">{siteDataEn.footer.productsTitle}</span>
            <span class="lang-zh">{siteDataZh.footer.productsTitle}</span>
          </h4>
          <ul>
            {siteDataEn.footer.productsLinks.map((link, idx) => (
              <li>
                <a href="#products">
                  <span class="lang-en">{link}</span>
                  <span class="lang-zh">{siteDataZh.footer.productsLinks[idx]}</span>
                </a>
              </li>
            ))}
          </ul>
        </div>

        <div class="footer-col">
          <h4>
            <span class="lang-en">{siteDataEn.footer.qualitiesTitle}</span>
            <span class="lang-zh">{siteDataZh.footer.qualitiesTitle}</span>
          </h4>
          <ul>
            {siteDataEn.footer.qualitiesList.map((item, idx) => (
              <li>
                <span>
                  <span class="lang-en">{item}</span>
                  <span class="lang-zh">{siteDataZh.footer.qualitiesList[idx]}</span>
                </span>
              </li>
            ))}
          </ul>
        </div>

        <div class="footer-col">
          <h4>
            <span class="lang-en">{siteDataEn.footer.headquartersTitle}</span>
            <span class="lang-zh">{siteDataZh.footer.headquartersTitle}</span>
          </h4>
          <ul class="contact-list">
            <li>
              <span class="lang-en">{siteDataEn.footer.address}</span>
              <span class="lang-zh">{siteDataZh.footer.address}</span>
            </li>
            <li>{siteDataEn.footer.tel}</li>
            <li>{siteDataEn.footer.email}</li>
          </ul>
        </div>
      </div>

      <div class="footer-bottom">
        <div class="footer-bottom-container">
          <p class="copyright">
            <span class="lang-en">{siteDataEn.footer.copyright}</span>
            <span class="lang-zh">{siteDataZh.footer.copyright}</span>
          </p>
          <div class="legal-links">
            <a href="#">
              <span class="lang-en">{siteDataEn.footer.privacyText}</span>
              <span class="lang-zh">{siteDataZh.footer.privacyText}</span>
            </a>
            <a href="#">
              <span class="lang-en">{siteDataEn.footer.icpText}</span>
              <span class="lang-zh">{siteDataZh.footer.icpText}</span>
            </a>
          </div>
        </div>
      </div>
    </footer>

    <!-- 弹窗 -->
    <div id="image-modal" class="image-modal">
      <div class="modal-content-wrapper">
        <button id="close-modal" class="modal-close-btn">✕</button>
        <button id="modal-prev" class="modal-nav-btn modal-prev-btn">‹</button>
        <img id="modal-img" src="" alt="大图" />
        <button id="modal-next" class="modal-nav-btn modal-next-btn">›</button>
        <div id="modal-counter" class="modal-counter">1 / 3</div>
      </div>
    </div>

    <!-- 8. 获取报价表单弹窗 (已提取至 siteData.ts，支持双语) -->
    <div id="quote-modal" class="quote-modal">
      <div class="quote-card">
        <button id="close-quote-modal" class="quote-close-btn">✕</button>
        <div class="quote-header">
          <h2>
            <span class="lang-en">{siteDataEn.quoteModal.title}</span>
            <span class="lang-zh">{siteDataZh.quoteModal.title}</span>
          </h2>
          <p>
            <span class="lang-en">{siteDataEn.quoteModal.subTitle}</span>
            <span class="lang-zh">{siteDataZh.quoteModal.subTitle}</span>
          </p>
        </div>
        <form id="quote-form" class="quote-form">
          <!-- Web3Forms 真实密钥保持不变 -->
          <input type="hidden" name="access_key" value="2795e4fe-dac3-4591-b6ae-90fd43f10fa4" />
          <input type="hidden" name="subject" value="New Battery Quote Inquiry - JC New Energy" />
          <input type="hidden" name="from_name" value="JC New Energy Website" />

          <div class="form-row">
            <div class="form-group">
              <label>
                <span class="lang-en">{siteDataEn.quoteModal.labels.name}</span>
                <span class="lang-zh">{siteDataZh.quoteModal.labels.name}</span>
              </label>
              <input 
                type="text" 
                name="name" 
                required 
                data-placeholder-en={siteDataEn.quoteModal.placeholders.name}
                data-placeholder-zh={siteDataZh.quoteModal.placeholders.name}
                placeholder={siteDataEn.quoteModal.placeholders.name} 
              />
            </div>
            <div class="form-group">
              <label>
                <span class="lang-en">{siteDataEn.quoteModal.labels.email}</span>
                <span class="lang-zh">{siteDataZh.quoteModal.labels.email}</span>
              </label>
              <input 
                type="email" 
                name="email" 
                required 
                data-placeholder-en={siteDataEn.quoteModal.placeholders.email}
                data-placeholder-zh={siteDataZh.quoteModal.placeholders.email}
                placeholder={siteDataEn.quoteModal.placeholders.email} 
              />
            </div>
          </div>

          <div class="form-row">
            <div class="form-group">
              <label>
                <span class="lang-en">{siteDataEn.quoteModal.labels.company}</span>
                <span class="lang-zh">{siteDataZh.quoteModal.labels.company}</span>
              </label>
              <input 
                type="text" 
                name="company" 
                required 
                data-placeholder-en={siteDataEn.quoteModal.placeholders.company}
                data-placeholder-zh={siteDataZh.quoteModal.placeholders.company}
                placeholder={siteDataEn.quoteModal.placeholders.company} 
              />
            </div>
            <div class="form-group">
              <label>
                <span class="lang-en">{siteDataEn.quoteModal.labels.country}</span>
                <span class="lang-zh">{siteDataZh.quoteModal.labels.country}</span>
              </label>
              <input 
                type="text" 
                name="country" 
                required 
                data-placeholder-en={siteDataEn.quoteModal.placeholders.country}
                data-placeholder-zh={siteDataZh.quoteModal.placeholders.country}
                placeholder={siteDataEn.quoteModal.placeholders.country} 
              />
            </div>
          </div>

          <div class="form-row grid-3">
            <div class="form-group">
              <label>
                <span class="lang-en">{siteDataEn.quoteModal.labels.voltage}</span>
                <span class="lang-zh">{siteDataZh.quoteModal.labels.voltage}</span>
              </label>
              <input 
                type="text" 
                name="voltage" 
                data-placeholder-en={siteDataEn.quoteModal.placeholders.voltage}
                data-placeholder-zh={siteDataZh.quoteModal.placeholders.voltage}
                placeholder={siteDataEn.quoteModal.placeholders.voltage} 
              />
            </div>
            <div class="form-group">
              <label>
                <span class="lang-en">{siteDataEn.quoteModal.labels.capacity}</span>
                <span class="lang-zh">{siteDataZh.quoteModal.labels.capacity}</span>
              </label>
              <input 
                type="text" 
                name="capacity" 
                data-placeholder-en={siteDataEn.quoteModal.placeholders.capacity}
                data-placeholder-zh={siteDataZh.quoteModal.placeholders.capacity}
                placeholder={siteDataEn.quoteModal.placeholders.capacity} 
              />
            </div>
            <div class="form-group">
              <label>
                <span class="lang-en">{siteDataEn.quoteModal.labels.quantity}</span>
                <span class="lang-zh">{siteDataZh.quoteModal.labels.quantity}</span>
              </label>
              <input 
                type="text" 
                name="quantity" 
                data-placeholder-en={siteDataEn.quoteModal.placeholders.quantity}
                data-placeholder-zh={siteDataZh.quoteModal.placeholders.quantity}
                placeholder={siteDataEn.quoteModal.placeholders.quantity} 
              />
            </div>
          </div>

          <div class="form-group">
            <label>
              <span class="lang-en">{siteDataEn.quoteModal.labels.requirements}</span>
              <span class="lang-zh">{siteDataZh.quoteModal.labels.requirements}</span>
            </label>
            <textarea 
              name="requirements" 
              rows="4" 
              required 
              data-placeholder-en={siteDataEn.quoteModal.placeholders.requirements}
              data-placeholder-zh={siteDataZh.quoteModal.placeholders.requirements}
              placeholder={siteDataEn.quoteModal.placeholders.requirements}
            ></textarea>
          </div>

          <button type="submit" class="btn-submit">
            <span class="lang-en">{siteDataEn.quoteModal.btnText}</span>
            <span class="lang-zh">{siteDataZh.quoteModal.btnText}</span>
          </button>
        </form>
      </div>
    </div>


    <!-- 9. 详细参数规格表弹窗 (顶栏平行规整排版) -->
    <div id="specs-modal" class="specs-modal">
      <div class="specs-card">
        
        <!-- 弹窗顶栏：左侧标题，右侧按钮组 -->
        <div class="specs-header">
          <div class="specs-title-box">
            <h2>
              <span class="lang-en">{siteDataEn.specsModal.title}</span>
              <span class="lang-zh">{siteDataZh.specsModal.title}</span>
            </h2>
            <p>
              <span class="lang-en">{siteDataEn.specsModal.subTitle}</span>
              <span class="lang-zh">{siteDataZh.specsModal.subTitle}</span>
            </p>
          </div>

          <!-- 右侧操作组：报价按钮与关闭按钮同一中线平行对齐 -->
          <div class="specs-header-actions">
            <button class="btn btn-green trigger-quote-modal" id="specs-to-quote-btn">
              <span class="lang-en">{siteDataEn.specsModal.inquireBtnText}</span>
              <span class="lang-zh">{siteDataZh.specsModal.inquireBtnText}</span>
            </button>
            <button id="close-specs-modal" class="specs-close-btn">✕</button>
          </div>
        </div>

        <!-- 详细参数对比表 -->
        <div class="table-responsive">
          <table class="specs-table">
            <thead>
              <tr>
                {siteDataEn.specsModal.headers.map((hEn, idx) => (
                  <th>
                    <span class="lang-en">{hEn}</span>
                    <span class="lang-zh">{siteDataZh.specsModal.headers[idx]}</span>
                  </th>
                ))}
              </tr>
            </thead>
            <tbody>
              {siteDataEn.specsModal.rows.map((rowEn, idx) => {
                const rowZh = siteDataZh.specsModal.rows[idx];
                return (
                  <tr>
                    <td class="spec-item-title">
                      <span class="lang-en">{rowEn.item}</span>
                      <span class="lang-zh">{rowZh.item}</span>
                    </td>
                    <td>{rowEn.v60}</td>
                    <td class="highlight-val">{rowEn.v90}</td>
                    <td class="remark-val">{rowEn.remark}</td>
                  </tr>
                );
              })}
            </tbody>
          </table>
        </div>

      </div>
    </div>
    
    <script src="../scripts/main.js"></script>
  </body>
</html>